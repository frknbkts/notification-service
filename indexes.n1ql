CREATE PRIMARY INDEX `#primary` ON `notification`.`app`.`notifications`;

CREATE INDEX `idx_notifications_user_created` 
ON `notification`.`app`.`notifications` (user_id, created_at DESC);

CREATE INDEX `idx_notifications_unread_count` 
ON `notification`.`app`.`notifications` (user_id) 
WHERE is_read = false;